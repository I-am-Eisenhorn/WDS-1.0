# HANDOFF.md

## Current frame

WDS 1.0 is a Windows 11 only AutoHotkey project.

Current codebase: AutoHotkey v1.1.
Target runtime: AutoHotkey v2.

The old code is a behavior reference. It is not the architecture target.

## Style

Write calmly and plainly.

Human-first means:

- credit the original project;
- keep notes understandable;
- avoid inflated language;
- avoid erasing prior work.

It does not mean adding ceremony or marketing language.

## Most important correction

Older notes may still describe AutoHotkey v1 compatibility, Windows 10 validation, or AutoHotkey v2 as optional. Treat those notes as stale.

## Next useful work

1. Update documentation to the new frame.
2. Inventory current v1 behavior.
3. Create the AutoHotkey v2 migration map.
4. Start the v2 port in small steps.
5. Test manually on Windows 11.

## Runtime note

The v2 implementation should be described as working only after a Windows 11 manual test with AutoHotkey v2 has been recorded.
